
    Какие сервисы уже реализованы? Например, это может быть только бэкенд, который принимает запросы, или фронтенд, который уже работает.

    Как эти сервисы взаимодействуют между собой? Например, фронтенд общается с бэкендом по REST API, а бэкенд что-то сохраняет в базу данных.

    Какие технологии ты используешь в уже реализованных частях? Например, Java с Spring Boot для бэкенда, React для фронтенда, PostgreSQL для базы данных.


Как работает текущий бэкенд? Ты упомянул, что он "готов именно сервинг". Можешь описать, как пользователь или другая система взаимодействует с ним? Он предоставляет REST API, куда можно отправить запрос с данными, чтобы получить предсказание от модели?

Как происходит регистрация моделей? Ты сказал, что реализован "реестр моделей". Как модель попадает в этот реестр? Пользователь загружает её через какой-то API? Если да, то какой формат модели ожидается (например, ONNX, TensorFlow SavedModel, или что-то другое)?

Как устроен список эндпоинтов? Каким образом пользователь получает доступ к развёрнутым моделям? Могут ли пользователи создавать свои собственные эндпоинты для разных моделей?

Используются ли KubeFlow Pipelines? Если ты работаешь с KubeFlow, возможно, ты используешь KubeFlow Pipelines для автоматизации рабочих процессов? Если да, то как они интегрированы с твоим сервисом?

Какой язык программирования ты используешь для своего бэкенда? Ты говорил о Java, но KubeFlow чаще всего ассоциируется с Python. Это кастомный сервис на Java, который управляет KubeFlow, или что-то другое?

Как происходит управление пользователями и их хранилищами?
Ты упомянул, что пользователь загружает файл в "пользовательское хранилище". Это отдельный сервис? Или это просто директория в файловой системе, которая привязана к пользователю? Как система понимает, кто есть кто?

Как работает микросервис Model Registry?
Ты сказал, что он регистрирует модель и создаёт Job в Kubernetes, которая копирует файл. Как он узнаёт, что модель готова для регистрации? Какие метаданные он сохраняет о модели (например, версия, автор, фреймворк)?

Что такое "Job" в Kubernetes, который копирует файл?
Это отдельный микросервис, который запускается по запросу от Model Registry? Или это скрипт, который вызывается через kubectl run? Как он получает доступ к пользовательскому хранилищу?

Как происходит создание эндпоинта?
Ты упомянул, что эндпоинт создаётся через API, который "по факту, это тоже Job". Как именно работает этот Job? Он создаёт Deployment, Service и Ingress в Kubernetes? Что происходит, если модель требует особых ресурсов, например, GPU?

Процесс копирования файла: Вы упомянули, что сервер "копирует оттуда файл". Это происходит синхронно, в рамках того же API-запроса, который инициирует пользователь? Или это асинхронный процесс, который запускается в фоновом режиме? Например, API-запрос возвращает ответ сразу, а копирование файла происходит позже.

Обработка ошибок при копировании: Что происходит, если файл слишком большой, или что-то пошло не так во время копирования? Как пользователь узнает о статусе операции? Есть ли какой-то механизм для отслеживания прогресса?

Идентификация пользователя и его хранилища: Когда пользователь делает запрос к API Model Registry, как система понимает, кто он? Используется ли какая-то система аутентификации (например, токены) для связывания запроса с конкретным пользователем и его S3-хранилищем?

Связь с Kubernetes: Вы ранее упоминали "Job в Kubernetes". В описанном процессе, где именно запускается этот Job? Он отвечает за копирование файла или за что-то другое?